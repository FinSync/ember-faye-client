/* eslint-env node */
'use strict';

module.exports = function(/* environment, appConfig */) {
  return { 
    faye: {
      URL: null,
      authToken: null,
      options: {        
      }
    }
  };
};
